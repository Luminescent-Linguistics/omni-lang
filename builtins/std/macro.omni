use std:ast:[Block Token Value]

; A loose approximation of syntax provided by the backend implementation for 
; `mac` (short for macro).
#pub
(define-keyword-function mac [Token:atom [Token,] Opt(Token:expr:ltrl:str) Token:expr])

#pub
(mac fun [Token:atom, [Token:atom Token:item:type,] Opt(Token:expr:ltrl:str) Token:expr]
    ; -- implementation of `fun` --
)
